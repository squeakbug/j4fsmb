# Medium
* Реализовать client так, чтобы можно было писать в сокет с нескольких потоков. 
  * Требуется обеспечить минимальную гранулярность на уровне запрос/ответ

# Low
* Написать недобенчмарк для SMB
* Сравнить задержки, общую пропускную способность (медианы, среднее, ...):
  * Без каналов, каждый фьючерс выполняет чтение из сокета, обработку, запись в сокет
  * Два канала между этими задачами
  * Четыре канала: сокет -> обработка, обработка -> взаимодействие с ФС, взаимодействие с ФС -> постобработка, постобработка -> запись в сокет
